# Maintenance policy

This Rust repository is the primary implementation of `oixc-proxy`.

Unless the user explicitly requests otherwise, implement product changes only
in this repository. The Go repository at `../oixc-go` is retained as a
compatibility reference and benchmark counterpart.
